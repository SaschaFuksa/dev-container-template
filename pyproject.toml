[project]
name = "dev-container-template"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.6.1"
]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
    "mypy",
]